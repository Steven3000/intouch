<div class="container">
  <% if notice.present? %>
  <p id="notice" class="button10b"><%= notice %></p>
  <% end %>
  <% if current_user.subscriptions.present? %>
  <h1 class="button10b"><%= current_user.username.capitalize %>'s Subscriptions</h1>
  <% end %>
  <%= render 'my_subs' %>
</div>

<div class="container">
  <h1 class="button10b">Select More Artists to Follow</h1>
<!--   <a class="button10c" href="mailto:contact.ntouchmusic@gmail.com">Your FAVORITE ARTIST not listed? Click here to let us know, ( contact.ntouchmusic@gmail.com ) then give us about a week.</a> -->
  <div class="row">
    <div class="col-xs-8">
      <%= form_tag subscriptions_path, method: "get" do %>
        <%= search_field_tag "search",
              params[:search],
              placeholder: "Search Artist",
              class: "form-control input-lg" %>
      <% end %>
    </div>
  </div>
</div>

<br>

<div class="container">
  <%= render 'new_subs' %>
</div>
