  <style type="text/css">
    body {
    background-image: url("https://s3-us-west-2.amazonaws.com/ntouchimages/admin/ang_chicago.JPG");
    background-repeat: no-repeat;
    width: 100%;
    }
  </style>

<div class="container">
  <% if notice.present? %>
  <p id="notice" class="button-bar"><%= notice %></p>
  <% end %>
  <% if current_user.subscriptions.present? %>
  <h1 class="button-bar navv-font"><%= current_user.username.capitalize %>'s Subscriptions <em>(Click on images for Artist Profile)</em></h1>
  <% end %>
  <%= render 'my_subs' %>
</div>

<div class="container">
  <h1 class="button-bar navv-font">Click on "Create Subscription" to follow any Artist</h1>
</div>

<br>

<div class="container">
  <%= render 'new_subs' %>
</div>
