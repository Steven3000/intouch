<div class="container">

  <h1>New Subscription</h1>


    <%= render 'form' %>
    </br>
    <%= link_to 'Back', subscriptions_path, :class => "btn btn-primary" %>

</div>

        <div class="btn-group btn-group-justified" role="group">
          <% subscription = Subscription.find_by({ :user_id => current_user.id, :artist_id => artist.id }) %>
          <% if favoriting.present? %>
            <%= link_to "Unsubscribe", favoriting, :method => "delete", :class => "btn btn-default" %>
          <% else %>
            <%= link_to "Subscribe", favoritings_url(:subscription => { :artist_id => artist.id }), :method => "post", :class => "btn btn-default" %>
          <% end %>
